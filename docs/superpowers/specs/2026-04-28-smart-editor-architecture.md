# Smart-Editor 系统架构图

| 项目 | 内容 |
|-----|------|
| 文档版本 | v1.0 |
| 创建日期 | 2026-04-28 |
| 适用范围 | smart-editor 全系统架构说明 |
| 配套文档 | 《智能文档编写工具 — 设计规格说明书》《本地知识库搭建框架文档》|

---

## 1. 整体系统架构

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                                    用户操作层                                            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │  拖入需求    │  │  搜索模板   │  │  编辑文档   │  │  一键校对   │  │  脱敏入库   │   │
│  │  文件上传    │  │  四维筛选   │  │  Tiptap    │  │  偏离检查   │  │  知识沉淀   │   │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘   │
└─────────┼────────────────┼────────────────┼────────────────┼────────────────┼──────────┘
          │                │                │                │                │
          ▼                ▼                ▼                ▼                ▼
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                              Tauri 桌面应用（统一窗口）                                    │
│  ┌───────────────────────────────────────────────────────────────────────────────────┐  │
│  │                           React 18 + TypeScript 前端                               │  │
│  │                                                                                    │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │  │
│  │  │  需求上传面板  │  │  模板库面板   │  │  智能推送面板 │  │      校对面板        │  │  │
│  │  │              │  │              │  │              │  │                      │  │  │
│  │  │ • 拖放上传   │  │ • 四维筛选   │  │ • 关键词触发 │  │ • 偏离项检测        │  │  │
│  │  │ • 要点提取   │  │ • 标签检索   │  │ • 章节模板   │  │ • 废标项检测        │  │  │
│  │  │ • 知识推荐   │  │ • 全文搜索   │  │ • 智能匹配   │  │ • 格式检查          │  │  │
│  │  │ • 草稿生成   │  │ • 预览/插入  │  │ • 实时推荐   │  │ • 术语一致性        │  │  │
│  │  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────────┬───────────┘  │  │
│  │         │                 │                 │                     │              │  │
│  │         └─────────────────┴─────────────────┴─────────────────────┘              │  │
│  │                                      │                                           │  │
│  │         ┌────────────────────────────────────────────────────────────┐           │  │
│  │         │              TipTap 富文本编辑器（ProseMirror）              │           │  │
│  │         │                                                            │           │  │
│  │         │  ┌────────────────────────────────────────────────────┐    │           │  │
│  │         │  │              Markdown / 富文本 编辑区                │    │           │  │
│  │         │  │                                                    │    │           │  │
│  │         │  │  • AI 生成草稿渲染   • 实时智能推送高亮             │    │           │  │
│  │         │  │  • 一键插入模板片段   • 自排版标题自动识别          │    │           │  │
│  │         │  │  • 复制到剪贴板      • 导出 Word / WPS             │    │           │  │
│  │         │  └────────────────────────────────────────────────────┘    │           │  │
│  │         └────────────────────────────────────────────────────────────┘           │  │
│  │                                                                                    │  │
│  │  ┌────────────────────────────────────────────────────────────────────────────┐  │  │
│  │  │                          UI 组件层（Tailwind CSS）                          │  │  │
│  │  │  工具栏 │ 侧边栏 │ 标签页 │ 对话框 │ 卡片 │ 表格 │ 搜索框 │ 筛选器 │ ...  │  │  │
│  │  └────────────────────────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────────────────────────┘  │
│                                          ▲                                              │
│                                          │ Tauri Bridge (IPC)                           │
│                                          ▼                                              │
│  ┌───────────────────────────────────────────────────────────────────────────────────┐  │
│  │                        Tauri Rust 后端（内嵌进程）                                   │  │
│  │                                                                                    │  │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────┐  │  │
│  │  │   NAS 扫描器     │  │  文档解析引擎    │  │  AI 客户端       │  │  脱敏引擎    │  │  │
│  │  │   (nas_scanner)  │  │  (docx/pdf/     │  │  (ai.rs)         │  │(desensitize)│  │  │
│  │  │                  │  │   xlsx/txt)     │  │                  │  │             │  │  │
│  │  │ • 目录遍历       │  │                  │  │ • Ollama 本地   │  │ • 客户名替换 │  │  │
│  │  │ • 文件名解析     │  │ • docx-rs      │  │   LLM 调用      │  │ • 金额脱敏   │  │  │
│  │  │ • 四维分类推断   │  │ • pdf-extract  │  │ • Claude API    │  │ • 日期脱敏   │  │  │
│  │  │ • 80GB+ 增量扫描 │  │ • calamine     │  │ • DeepSeek API  │  │ • 人员脱敏   │  │  │
│  │  │                  │  │ • 分段提取      │  │ • 自适应切换    │  │ • 系统名脱敏 │  │  │
│  │  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘  └──────┬──────┘  │  │
│  │           │                    │                    │                   │         │  │
│  │           └────────────────────┴────────────────────┴───────────────────┘         │  │
│  │                                       │                                            │  │
│  │  ┌────────────────────────────────────┴────────────────────────────────────────┐  │  │
│  │  │                           搜索引擎 & 数据库层                                  │  │  │
│  │  │                                                                             │  │  │
│  │  │   ┌─────────────────────────┐        ┌─────────────────────────────────┐   │  │  │
│  │  │   │   Meilisearch           │        │   SQLite (rusqlite)             │   │  │  │
│  │  │   │   全文搜索引擎           │◄──────►│   元数据 & 四维分类数据库        │   │  │  │
│  │  │   │                         │        │                                 │   │  │  │
│  │  │   │ • 中文分词              │        │ • templates 表（模板卡片）       │   │  │  │
│  │  │   │ • 容错拼写              │        │ • 四维分类索引                   │   │  │  │
│  │  │   │ • 相关性排序            │        │ • 标签体系                       │   │  │  │
│  │  │   │ • 过滤/排序/高亮        │        │ • 使用统计                       │   │  │  │
│  │  │   │ • 增量索引更新          │        │ • 版本记录                       │   │  │  │
│  │  │   └─────────────────────────┘        └─────────────────────────────────┘   │  │  │
│  │  └─────────────────────────────────────────────────────────────────────────────┘  │  │
│  │                                                                                    │  │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────────────────────┐│  │
│  │  │  偏离检测引擎    │  │  格式检查引擎    │  │  剪贴板 / Office.js 桥接层           ││  │
│  │  │ (deviation.rs)  │  │ (punctuation.rs)│  │                                      ││  │
│  │  │                 │  │                 │  │ • 富文本 HTML → 系统剪贴板            ││  │
│  │  │ • 技术偏离表    │  │ • 标点符号规范  │  │ • Office.js 可选插件（深度集成）      ││  │
│  │  │ • 商务偏离表    │  │ • 编号连续性    │  │ • 向 Word/WPS 插入内容               ││  │
│  │  │ • 废标项检测    │  │ • 字体/段落格式 │  │                                      ││  │
│  │  │ • 敏感词检查    │  │ • 术语一致性    │  │                                      ││  │
│  │  └─────────────────┘  └─────────────────┘  └─────────────────────────────────────┘│  │
│  └───────────────────────────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────┬─────────────────────────────────────────────┘
                                            │
              ┌─────────────────────────────┼─────────────────────────────┐
              │                             │                             │
              ▼                             ▼                             ▼
     ┌─────────────────┐          ┌─────────────────┐          ┌─────────────────┐
     │   NAS 共享盘     │          │   Ollama        │          │   云端 API      │
     │   (SMB/CIFS)    │          │   (本地 LLM)    │          │   (降级备用)    │
     │                 │          │                 │          │                 │
     │ • 80GB+ 历史文档│          │ • qwen2.5:14b   │          │ • Claude API    │
     │ • 四维目录结构  │          │ • deepseek-     │          │ • DeepSeek API  │
     │ • 自动扫描解析  │          │   r1:14b        │          │                 │
     │                 │          │ • 数据不出本地  │          │ • 低配机器降级  │
     └─────────────────┘          └─────────────────┘          └─────────────────┘
```

---

## 2. 模块依赖关系

```
                              ┌─────────────────────┐
                              │    用户界面 (UI)     │
                              │  React + Tailwind   │
                              └──────────┬──────────┘
                                         │
                     ┌───────────────────┼───────────────────┐
                     │                   │                   │
                     ▼                   ▼                   ▼
           ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
           │  需求上传面板    │ │  模板库面板      │ │  校对面板        │
           │  Upload Panel   │ │  Library Panel  │ │  Review Panel   │
           └────────┬────────┘ └────────┬────────┘ └────────┬────────┘
                    │                   │                   │
                    └───────────────────┼───────────────────┘
                                        │
                                        ▼
                           ┌────────────────────────┐
                           │  TipTap 编辑器核心层    │
                           │  (ProseMirror 引擎)     │
                           └────────────┬───────────┘
                                        │
                    ┌───────────────────┼───────────────────┐
                    │                   │                   │
                    ▼                   ▼                   ▼
           ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
           │  Tauri Bridge   │ │  智能推送模块    │ │  自排版模块      │
           │  (IPC Command)  │ │  (Keyword/AI)   │ │  (Auto Format)  │
           └────────┬────────┘ └────────┬────────┘ └─────────────────┘
                    │                   │
                    │         ┌─────────┴─────────┐
                    │         │                   │
                    ▼         ▼                   ▼
           ┌─────────────────────────────────────────────────────────┐
           │                  Rust 后端服务层                         │
           │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐      │
           │  │nas_scan │ │ parser  │ │  ai.rs  │ │search.rs│      │
           │  └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘      │
           │       │           │           │           │            │
           │       └───────────┴─────┬─────┴───────────┘            │
           │                         │                              │
           │  ┌──────────────────────┼──────────────────────┐       │
           │  │                      │                      │       │
           │  ▼                      ▼                      ▼       │
           │ ┌───────────┐    ┌───────────┐    ┌───────────┐      │
           │ │desensitize│    │deviation  │    │punctuation│      │
           │ │   .rs     │    │   .rs     │    │   .rs     │      │
           │ └─────┬─────┘    └─────┬─────┘    └─────┬─────┘      │
           │       │                │                │            │
           │       └────────────────┼────────────────┘            │
           │                        │                             │
           │                        ▼                             │
           │           ┌─────────────────────┐                   │
           │           │   config.rs (配置)   │                   │
           │           └─────────────────────┘                   │
           └─────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┼───────────────┐
                    │               │               │
                    ▼               ▼               ▼
            ┌───────────┐  ┌───────────┐  ┌───────────┐
            │  SQLite   │  │Meilisearch│  │ 外部服务   │
            │  元数据库  │  │ 全文索引  │  │ Ollama/API│
            └───────────┘  └───────────┘  └───────────┘
```

---

## 3. 核心数据流（7步工作流）

```
   用户操作                                          系统处理
      │                                                 │
      │   ┌──────────────────────────────────────────────────────────────┐
      │   │                                                              │
      ▼   ▼                                                              │
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│ 1.需求上传   │ ──► │ 2.要点提取   │ ──► │ 3.知识检索   │ ──► │ 4.草稿生成   │
│   & 解析    │     │   (AI)      │     │ (Meilisearch)│     │   (AI)      │
└──────┬──────┘     └──────┬──────┘     └──────┬──────┘     └──────┬──────┘
       │                   │                   │                   │
       │  拖入文件          │  调用 Ollama/     │  要点→查询词      │  组装 Prompt
       │  ├─ docx          │  云端 API         │  ├─ 全文检索      │  (要点+引用+
       │  ├─ pdf           │  ├─ 技术要点       │  ├─ 去重排序      │   框架)
       │  ├─ xlsx          │  ├─ 商务要点       │  └─ Top-10 返回   │  AI 生成 Markdown
       │  └─ txt           │  └─ 可编辑勾选     │                   │
       │                   │     列表           │                   │
       │                   │                   │                   │
       │              ┌────┴───────────────────┴───────────────────┘
       │              │
       ▼              ▼
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│ 7.知识沉淀   │ ◄── │ 6.自动校对   │ ◄── │ 5.人工编辑   │ ◄── │  Tiptap    │
│ (脱敏入库)  │     │ (AI + 规则)  │     │   & 完善    │     │  编辑器     │
└─────────────┘     └─────────────┘     └─────────────┘     └──────┬──────┘
       │                                                           │
       │                                                           │
       │  脱敏引擎处理               偏离/废标/格式/术语              │  富文本编辑
       │  ├─ 客户名 → 占位符         规则检查 + AI 语义分析           │  ├─ 增删改
       │  ├─ 金额 → 区间             问题列表 + 修复建议              │  ├─ 调格式
       │  ├─ 日期 → 占位符            导出校对报告                    │  ├─ 插入模板
       │  └─ 系统名 → 通用名                                          │  └─ 一键插入 Word
       │                                                           │
       │                    ┌─────────────────────┐                 │
       └────────────────────┤  存入知识库（四维分类） │◄────────────────┘
                            │  SQLite + Meilisearch │
                            └─────────────────────┘
```

---

## 4. 知识库 NAS 目录架构（四维分类映射）

```
/Volumes/NAS/知识库/                              # 根目录（项目阶段维度）
│
├── 00-通用素材/                                   # 跨阶段复用素材
│   ├── 01-公司资质/
│   │   ├── 营业执照/
│   │   ├── 资质证书/                              # 等保测评、CNCERT、ISO
│   │   ├── 安全资质/                              # 安全服务资质、产品资质
│   │   └── 人员资质/                              # 工程师证书、专家简历
│   ├── 02-案例库/
│   │   ├── 金融行业/
│   │   ├── 政府行业/
│   │   ├── 能源行业/
│   │   ├── 医疗行业/
│   │   └── 教育行业/
│   ├── 03-产品资料/
│   │   ├── 防火墙/
│   │   ├── WAF/
│   │   ├── 态势感知/
│   │   ├── SOC/
│   │   ├── 漏洞扫描/
│   │   └── 数据防泄漏/
│   └── 04-商务模板/
│       ├── 报价单模板/
│       ├── 合同模板/
│       ├── 承诺函模板/
│       └── 授权书模板/
│
├── 01-方案阶段/                                   # 项目前期
│   ├── 01-网络安全/                               # 业务领域维度
│   │   ├── 01-技术方案/                           # 内容模块维度
│   │   │   └── 【技术方案】[防御] 防火墙部署设计 - 某银行项目.docx
│   │   ├── 02-实施方案/
│   │   └── 03-实施计划/
│   ├── 02-应用安全/
│   ├── 03-数据安全/
│   ├── 04-安全运营/
│   └── 05-安全管理/
│
├── 02-投标阶段/                                   # 项目中期
│   ├── 01-网络安全/
│   │   ├── 01-技术方案/
│   │   │   ├── 【技术方案】[防御] 防火墙部署方案 - 某银行项目.docx
│   │   │   └── 【投标应答】 等保2.0三级技术方案 - 点对点应答.docx
│   │   ├── 02-商务条款/
│   │   │   ├── 【商务条款】 年度安全运维服务报价单.xlsx
│   │   │   └── 【商务条款】 三年维保服务报价 - 简化版.xlsx
│   │   ├── 03-偏离说明/
│   │   │   └── 【技术方案】 技术偏离表 - 某银行项目.docx
│   │   ├── 04-资质证明/
│   │   │   ├── 【投标应答】 公司近三年业绩清单.xlsx
│   │   │   └── 【投标应答】 项目经理简历 - 张工.docx
│   │   └── 05-投标应答/
│   ├── 02-应用安全/
│   ├── 03-数据安全/
│   ├── 04-安全运营/
│   └── 05-安全管理/
│
└── 03-合同阶段/                                   # 项目后期
    ├── 01-网络安全/
    │   ├── 01-技术方案/
    │   ├── 02-商务条款/
    │   └── 03-实施计划/
    ├── 02-应用安全/
    ├── 03-数据安全/
    ├── 04-安全运营/
    └── 05-安全管理/
```

### 四维分类映射规则

```
┌──────────────────────────────────────────────────────────────────────┐
│                        四维分类推断引擎                               │
│                                                                      │
│  文件路径                        │  文件名                           │
│  ┌──────────────────────────┐   │  ┌──────────────────────────────┐ │
│  │ 01-方案阶段/              │   │  │ 【技术方案】[防御] 标题...   │ │
│  │      ▲                   │   │  │     ▲        ▲              │ │
│  │      └─ 项目阶段          │   │  │     │        └─ 安全层级    │ │
│  │                          │   │  │     └─ 文档属性              │ │
│  │ 01-网络安全/              │   │  └──────────────────────────────┘ │
│  │      ▲                   │   │                                   │
│  │      └─ 业务领域          │   │                                   │
│  │                          │   │                                   │
│  │ 01-技术方案/              │   │                                   │
│  │      ▲                   │   │                                   │
│  │      └─ 内容模块          │   │                                   │
│  └──────────────────────────┘   │                                   │
│                                 │                                   │
│  推断准确率：路径 ~85% │ 路径+文件名 ~90% │ +AI 辅助 ~95%            │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 5. 前端组件架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         App.tsx（应用根组件）                            │
└─────────────────────────────────┬───────────────────────────────────────┘
                                  │
        ┌─────────────────────────┼─────────────────────────┐
        │                         │                         │
        ▼                         ▼                         ▼
┌───────────────┐       ┌───────────────┐       ┌───────────────┐
│  TabBar       │       │  MainLayout   │       │  StatusBar    │
│  (标签导航)    │       │  (主内容区)    │       │  (状态栏)      │
└───────┬───────┘       └───────┬───────┘       └───────────────┘
        │                       │
        │           ┌───────────┼───────────┐
        │           │           │           │
        │           ▼           ▼           ▼
        │   ┌─────────────┐ ┌─────────┐ ┌─────────────┐
        │   │ UploadTab   │ │LibTab   │ │ReviewTab    │
        │   │ (需求上传)   │ │(模板库)  │ │(校对面板)   │
        │   └──────┬──────┘ └────┬────┘ └──────┬──────┘
        │          │             │             │
        │          │     ┌───────┴───────┐     │
        │          │     │               │     │
        │          │     ▼               ▼     │
        │          │ ┌───────────┐  ┌────────┐ │
        │          │ │SearchPanel│  │Filter  │ │
        │          │ │(搜索面板)  │  │Panel   │ │
        │          │ └─────┬─────┘  │(筛选器)│ │
        │          │       │        └────────┘ │
        │          │       ▼                   │
        │          │ ┌───────────────┐         │
        │          │ │TemplateCard   │         │
        │          │ │(模板卡片列表)  │         │
        │          │ └───────────────┘         │
        │          │                           │
        └──────────┴───────────────────────────┘
                     │
                     ▼
         ┌─────────────────────┐
         │   TipTapEditor      │
         │  (富文本编辑器核心)  │
         │                     │
         │  ┌───────────────┐  │
         │  │ SlashMenu     │  │  ──► / 命令菜单
         │  │ (斜杠菜单)     │  │
         │  ├───────────────┤  │
         │  │ BubbleMenu    │  │  ──► 划线弹出菜单
         │  │ (气泡菜单)     │  │
         │  ├───────────────┤  │
         │  │ SmartPush     │  │  ──► 智能推送浮层
         │  │ (智能推荐)     │  │
         │  ├───────────────┤  │
         │  │ ClipboardBridge│ │  ──► 剪贴板桥接
         │  │ (剪贴板桥接)   │  │
         │  └───────────────┘  │
         └─────────────────────┘
```

---

## 6. Rust 后端模块架构

```
src-tauri/src/
│
├── main.rs                 # Tauri 应用入口
│   ├── 注册 Tauri Commands
│   ├── 初始化 Meilisearch 进程
│   ├── 初始化 SQLite 连接池
│   └── 启动系统托盘/窗口管理
│
├── lib.rs                  # 公共模块导出
│
├── ai.rs                   # AI 客户端模块 ──────────────────────┐
│   ├── ask_local(prompt)    # → Ollama HTTP API                │
│   ├── ask_cloud(prompt)    # → Claude/DeepSeek API            │
│   ├── extract_requirements(text, doc_type)  # 要点提取         │
│   ├── generate_draft(requirements, refs)    # 草稿生成         │
│   └── adaptive_strategy()  # 自适应策略（本地优先→降级）       │
│                                                                │
├── db.rs                   # 数据库模块 ─────────────────────────┤
│   ├── init_db()            # 初始化 SQLite 表结构             │
│   ├── template CRUD        # 模板增删改查                     │
│   ├── four_dim_query()     # 四维分类组合查询                  │
│   └── stats_update()       # 使用统计更新                     │
│                                                                │
├── search.rs               # 搜索引擎模块 ───────────────────────┤
│   ├── init_meili()         # 启动/连接 Meilisearch             │
│   ├── index_documents()    # 文档批量索引                     │
│   ├── search(query, filters) # 全文检索+过滤                  │
│   └── update_index()       # 增量索引更新                     │
│                                                                │
├── nas_scanner.rs          # NAS 扫描模块 ───────────────────────┤
│   ├── scan_directory(path) # 递归扫描目录                     │
│   ├── parse_filename(name) # 文件名解析（属性/层级推断）        │
│   ├── infer_four_dims(path, name) # 四维分类推断              │
│   └── incremental_scan()   # 增量扫描（基于 mtime）             │
│                                                                │
├── deviation.rs            # 偏离检测模块 ───────────────────────┤
│   ├── check_tech_deviation(bid_doc, req_doc)  # 技术偏离      │
│   ├── check_biz_deviation(bid_doc, req_doc)   # 商务偏离      │
│   ├── check_disqualification(bid_doc)         # 废标项检测     │
│   └── check_sensitive_words(bid_doc)          # 敏感词检测     │
│                                                                │
├── desensitize.rs          # 脱敏引擎模块 ───────────────────────┤
│   ├── desensitize_client_name(text)           # 客户名脱敏     │
│   ├── desensitize_amount(text)                # 金额脱敏       │
│   ├── desensitize_date(text)                  # 日期脱敏       │
│   ├── desensitize_system(text)                # 系统名脱敏     │
│   └── desensitize_person(text)                # 人员脱敏       │
│                                                                │
├── punctuation.rs          # 格式检查模块 ───────────────────────┤
│   ├── check_punctuation(text)                 # 标点规范       │
│   ├── check_numbering(doc)                    # 编号连续性     │
│   ├── check_font_format(doc)                  # 字体格式       │
│   └── check_term_consistency(doc)             # 术语一致性     │
│                                                                │
├── config.rs               # 配置管理模块 ───────────────────────┤
│   ├── load_config()        # 读取用户配置                     │
│   ├── save_config()        # 保存用户配置                     │
│   ├── ai_config()          # AI 相关配置                      │
│   └── nas_config()         # NAS 路径配置                     │
│                                                                │
└── clipboard.rs            # 剪贴板桥接模块 ─────────────────────┘
    ├── copy_rich_text(html)   # 富文本写入剪贴板
    ├── copy_markdown(md)      # Markdown 写入剪贴板
    └── office_js_bridge()     # Office.js 插件桥接（可选）
```

---

## 7. 数据模型关系

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           SQLite 数据模型                                │
│                                                                         │
│   ┌───────────────┐                                                     │
│   │  templates    │                                                     │
│   │  (模板卡片表)  │                                                     │
│   ├───────────────┤                                                     │
│   │ id            │ ◄── PK                                              │
│   │ title         │                                                     │
│   │ content       │ ──► Markdown 正文                                    │
│   │ content_html  │ ──► 预渲染 HTML                                      │
│   │ doc_attr      │ ──► 文档属性 {投标应答,技术方案,实施方案,合同协议,汇报材料}│
│   │ business_domain│ ──► 业务领域 {网络安全,应用安全,数据安全,安全运营,安全管理}│
│   │ security_layer│ ──► 安全层级 {检测,防御,分析,治理}                     │
│   │ content_module│ ──► 内容模块 {技术方案,商务条款,实施计划,资质证明,偏离说明,案例介绍}│
│   │ project_phase │ ──► 项目阶段 {方案阶段,投标阶段,合同阶段}                │
│   │ tags          │ ──► JSON 数组标签                                    │
│   │ source_file   │ ──► 来源 NAS 路径                                     │
│   │ source_para_range│ ──► 来源段落范围                                  │
│   │ created_at    │                                                     │
│   │ updated_at    │                                                     │
│   │ use_count     │ ──► 使用次数统计                                     │
│   │ rating        │ ──► 评分                                             │
│   └───────┬───────┘                                                     │
│           │                                                             │
│           │ 1:N                                                         │
│           ▼                                                             │
│   ┌───────────────┐                                                     │
│   │ template_tags │                                                     │
│   │ (标签关联表)   │                                                     │
│   ├───────────────┤                                                     │
│   │ template_id   │ ◄── FK                                              │
│   │ tag_name      │                                                     │
│   └───────────────┘                                                     │
│                                                                         │
│   ┌───────────────┐                                                     │
│   │   config      │                                                     │
│   │  (配置表)      │                                                     │
│   ├───────────────┤                                                     │
│   │ key           │                                                     │
│   │ value         │                                                     │
│   └───────────────┘                                                     │
│                                                                         │
│   ┌───────────────┐                                                     │
│   │ scan_logs     │                                                     │
│   │ (扫描日志表)   │                                                     │
│   ├───────────────┤                                                     │
│   │ file_path     │                                                     │
│   │ scan_time     │                                                     │
│   │ file_hash     │                                                     │
│   │ parse_status  │                                                     │
│   └───────────────┘                                                     │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                        Meilisearch 索引结构                              │
│                                                                         │
│   Index: "templates"                                                    │
│   ├── primaryKey: "id"                                                  │
│   ├── searchableAttributes:                                             │
│   │   ├── "title"        (权重: 最高)                                    │
│   │   ├── "content"      (权重: 高)                                      │
│   │   └── "tags"         (权重: 中)                                      │
│   ├── filterableAttributes:                                             │
│   │   ├── "doc_attr"                                                    │
│   │   ├── "business_domain"                                             │
│   │   ├── "security_layer"                                              │
│   │   ├── "content_module"                                              │
│   │   ├── "project_phase"                                               │
│   │   └── "tags"                                                        │
│   ├── sortableAttributes:                                               │
│   │   ├── "use_count"    (使用次数排序)                                  │
│   │   ├── "rating"       (评分排序)                                      │
│   │   └── "updated_at"   (更新时间排序)                                  │
│   └── rankingRules:                                                     │
│       ├── "words"                                                       │
│       ├── "typo"         (容错拼写)                                      │
│       ├── "proximity"                                                   │
│       ├── "attribute"                                                   │
│       ├── "sort"                                                        │
│       └── "exactness"                                                   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 8. AI 调用链路

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            AI 自适应策略                                 │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        决策引擎                                  │   │
│  │                                                                  │   │
│  │   Ollama 可用? ──No──► 提示安装 或 询问切换云端                   │   │
│  │        │                                                         │   │
│  │       Yes                                                        │   │
│  │        │                                                         │   │
│  │        ▼                                                         │   │
│  │   响应超时 > 60s? ──Yes──► 自动降级到云端 API + 提示用户          │   │
│  │        │                                                         │   │
│  │       No                                                         │   │
│  │        │                                                         │   │
│  │        ▼                                                         │   │
│  │   输出 token > 4000? ──Yes──► 建议切换云端以获得更好效果          │   │
│  │        │                                                         │   │
│  │       No                                                         │   │
│  │        │                                                         │   │
│  │        ▼                                                         │   │
│  │   用户显式选择云端? ──Yes──► 走云端 API                           │   │
│  │        │                                                         │   │
│  │       No                                                         │   │
│  │        │                                                         │   │
│  │        ▼                                                         │   │
│  │   ┌─────────────┐                                               │   │
│  │   │ 本地 Ollama  │ ◄── 默认优先路径（数据不出本地）              │   │
│  │   │  qwen2.5:14b│                                               │   │
│  │   │ deepseek-r1 │                                               │   │
│  │   └─────────────┘                                               │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        Prompt 管线                               │   │
│  │                                                                  │   │
│  │   需求要点提取 ──► 技术文档 Prompt / 商务文档 Prompt              │   │
│  │        │                                                         │   │
│  │        ▼                                                         │   │
│  │   知识库检索 ──► 要点 → Meilisearch 查询 → Top-10 结果           │   │
│  │        │                                                         │   │
│  │        ▼                                                         │   │
│  │   草稿生成 ──► 技术方案 Prompt / 商务响应 Prompt                  │   │
│  │        │                                                         │   │
│  │        ▼                                                         │   │
│  │   校对分析 ──► 偏离检测 + 废标检查 + 格式检查 Prompt              │   │
│  │                                                                  │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 9. 部署架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          单包部署模式（Tauri）                           │
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐  │
│   │                      安装包构建输出                              │  │
│   │                                                                  │  │
│   │   Windows:  smart-editor-setup.exe  (< 10MB)                    │  │
│   │   macOS:    smart-editor.dmg       (< 10MB)                     │  │
│   │                                                                  │  │
│   │   内嵌内容：                                                      │  │
│   │   ├── Tauri Runtime (Rust + WebView2/WebKit)                    │  │
│   │   ├── React 前端静态资源                                         │  │
│   │   ├── Meilisearch 可执行文件                                     │  │
│   │   └── SQLite 运行时库                                           │  │
│   │                                                                  │  │
│   │   零依赖：用户无需安装 Python、Node.js、数据库等                  │  │
│   │   双击安装 → 自动初始化索引 → 即可使用                           │  │
│   └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐  │
│   │                      运行时依赖（外部）                          │  │
│   │                                                                  │  │
│   │   可选依赖（推荐安装）：                                          │  │
│   │   ├── Ollama（本地 LLM，数据不出本地）                           │  │
│   │   │   └── 模型: qwen2.5:14b / deepseek-r1:14b                   │  │
│   │   │   └── 安装: 官网下载 ollama.com                             │  │
│   │   │                                                            │  │
│   │   ├── NAS 共享盘（SMB/CIFS 挂载）                                │  │
│   │   │   └── 路径: /Volumes/NAS/知识库/ 或 \\NAS\知识库\           │  │
│   │   │                                                            │  │
│   │   └── Word / WPS（剪贴板桥接或 Office.js 插件）                  │  │
│   │       └── Word 2016+ / WPS 2019+                               │  │
│   │                                                                  │  │
│   │   降级依赖（本地不可用时）：                                      │  │
│   │   └── Claude API Key / DeepSeek API Key                        │  │
│   │                                                                │  │
│   └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐  │
│   │                      本地数据存储                                │  │
│   │                                                                  │  │
│   │   ~/.smart-editor/                                               │  │
│   │   ├── config.json           # 用户配置                           │  │
│   │   ├── data.db               # SQLite 数据库                      │  │
│   │   ├── meili_data/           # Meilisearch 索引数据               │  │
│   │   └── temp/                 # 临时文件（解析后自动清理）         │  │
│   │                                                                  │  │
│   └─────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 10. 技术栈总览

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           Smart-Editor 技术栈                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        前端层                                    │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐      │   │
│  │  │ React 18 │  │TypeScript│  │TipTap    │  │Tailwind  │      │   │
│  │  │          │  │          │  │ProseM.   │  │CSS       │      │   │
│  │  │ 组件渲染  │  │ 类型安全  │  │富文本编辑│  │样式框架  │      │   │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘      │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                    │                                    │
│  ┌─────────────────────────────────▼─────────────────────────────────┐ │
│  │                     桌面框架层 (Tauri)                             │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │ │
│  │  │  Rust    │  │ WebView2 │  │  Tokio   │  │  IPC     │        │ │
│  │  │  后端逻辑 │  │/ WebKit  │  │ 异步运行时│  │ 通信桥接  │        │ │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘        │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                    │                                    │
│  ┌─────────────────────────────────▼─────────────────────────────────┐ │
│  │                      数据处理层                                    │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │ │
│  │  │docx-rs   │  │pdf-extract│  │calamine  │  │rusqlite  │        │ │
│  │  │docx 解析 │  │pdf 解析  │  │xlsx 解析 │  │SQLite 操作│        │ │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘        │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                    │                                    │
│  ┌─────────────────────────────────▼─────────────────────────────────┐ │
│  │                      搜索与存储层                                  │ │
│  │  ┌────────────────────────┐  ┌────────────────────────┐        │ │
│  │  │     Meilisearch        │  │       SQLite           │        │ │
│  │  │  全文搜索引擎(内嵌进程) │  │   元数据数据库          │        │ │
│  │  │  • 中文分词            │  │   • 零配置              │        │ │
│  │  │  • 容错搜索            │  │   • 单文件              │        │ │
│  │  │  • 相关性排序          │  │   • 跨平台              │        │ │
│  │  └────────────────────────┘  └────────────────────────┘        │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                    │                                    │
│  ┌─────────────────────────────────▼─────────────────────────────────┐ │
│  │                      AI 服务层                                     │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │ │
│  │  │   Ollama     │  │ Claude API   │  │ DeepSeek API │           │ │
│  │  │  (本地优先)   │  │  (云端降级)   │  │  (云端降级)   │           │ │
│  │  │ • qwen2.5:14b│  │              │  │              │           │ │
│  │  │ • deepseek   │  │              │  │              │           │ │
│  │  └──────────────┘  └──────────────┘  └──────────────┘           │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                    │                                    │
│  ┌─────────────────────────────────▼─────────────────────────────────┐ │
│  │                      外部系统层                                    │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │ │
│  │  │   NAS 共享盘  │  │  Word/WPS    │  │  操作系统    │           │ │
│  │  │  SMB/CIFS    │  │  Office.js   │  │  Win/Mac     │           │ │
│  │  └──────────────┘  └──────────────┘  └──────────────┘           │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 附录：架构图索引

| 图号 | 名称 | 说明 |
|-----|------|------|
| 图1 | 整体系统架构 | 前端、后端、外部依赖的全貌 |
| 图2 | 模块依赖关系 | 各模块间的调用和依赖关系 |
| 图3 | 核心数据流 | 7步工作流的完整数据流转 |
| 图4 | 知识库 NAS 目录架构 | 四维分类对应的物理目录结构 |
| 图5 | 前端组件架构 | React 组件层级和交互关系 |
| 图6 | Rust 后端模块架构 | src-tauri/src/ 下的模块划分 |
| 图7 | 数据模型关系 | SQLite 表结构 + Meilisearch 索引 |
| 图8 | AI 调用链路 | 自适应策略和 Prompt 管线 |
| 图9 | 部署架构 | 单包部署模式及运行时依赖 |
| 图10 | 技术栈总览 | 完整技术选型一览 |
